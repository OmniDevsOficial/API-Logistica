server:
  port: 8081

spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: operacional-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/newelog_db?currentSchema=operacional}
    username: ${SPRING_DATASOURCE_USERNAME:postgresql}
    password: ${SPRING_DATASOURCE_PASSWORD:DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: operacional
  flyway:
    schemas: operacional
    create-schemas: true
